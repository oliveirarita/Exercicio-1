classe Pilha:
    def __init __ (auto-):
        self.itens = list ()
    def isEmpty (self, item):
        se len (pilha) == 0:
            retornar verdadeiro
        retorna falso
    def push (self, item):
        self.itens.append (item)
    def pop (self):
        self.itens.pop ()
    def peek (auto-):
        self.itens [-1]
    comprimento def (auto, item):
        return len (self.itens)

pilha = Pilha ()
pilha.push (12)
pilha.push (20)
pilha.push (35)
pilha.push (14)
pilha.push (5)
pilha.pop ()
imprimir (pilha.itens)
